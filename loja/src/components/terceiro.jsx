import React from "react";
import { StyleSheet,Text } from "react-native";

export default() =>{
    return <Text style="{fontG}">O primeiro de A é maior que o valor de B</Text>
}
const estilo = StyleSheet.create({
    fontG:{
        fontSize: 30,
    }
    
}) 